#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of a number
 *
 * Return: returns the absolute value of the number
 */
int _abs(int c)
{
	int value = abs(c);

	return (value);
}
