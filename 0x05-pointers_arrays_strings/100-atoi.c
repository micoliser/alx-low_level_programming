#include <stdlib.h>
#include "main.h"

/**
 * _atoi - converts a string to an integer
 * @s: the pointer to the string
 *
 * Return: the integer
 */
int _atoi(char *s)
{
	int val = atoi(s);

	return (val);
}
